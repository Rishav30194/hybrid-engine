# Copy to .env.local and fill in your Supabase project values.
# Both are safe to expose in a client bundle (the anon key is publishable;
# Row-Level Security protects the data). Cloud sync stays disabled until set.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
