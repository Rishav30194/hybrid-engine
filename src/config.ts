/**
 * Hardcoded option defaults (see docs/IMPLEMENTATION_PHASES.md).
 * The prototype exposed these as component props; we ship the defaults.
 */

/** Show the "· nn%" tail on main-lift meta lines. */
export const SHOW_PERCENTS = true

/** Enlarge the main-lift load numerals (40px → 52px). */
export const BIG_LOAD = false
