/**
 * Hardcoded option defaults (see docs/IMPLEMENTATION_PHASES.md).
 * There is deliberately no settings UI.
 */

/** Show the "· nn%" tail on main-lift meta lines. */
export const SHOW_PERCENTS = true
